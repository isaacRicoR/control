import React from "react";
import SettingsView from "@ui/views/settings/SettingsView";

/**
 * SettingsPage — Página de Centro de Control (Configuración).
 * 
 * Server Component que delega la UI interactiva a un Client Component.
 */
export default function SettingsPage() {
    return <SettingsView />;
}
